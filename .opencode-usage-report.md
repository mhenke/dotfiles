# OpenCode Usage Report — 2026-08-09

## 0. Data Quality & Gaps

- **Daily log gaps:** 7 days missing in 90-day window — May 20-21, Jul 25-27 (3-day), Aug 2, Aug 7. ~7 days of activity under-represents 90-day estimates.
- **Hourly truncation today:** Most recent entry 18:00 UTC; report generated 23:25 UTC — ~5h of today's burn missing.
- **NVIDIA NIM catalog:** Fetched 2026-07-30 (10d old), all 47 models parseable.
- **Billing coverage:** DB Zen paid estimate **$94.31** (4,873 steps, 20 models) is *below* workspace billing **$127.82** (ground truth, 9 payments). DB usually inflates; here it undercounts — the workspace billing spans May 27 → Aug 9 (predates DB window start of May 11), and Go-cap consumption can route via card-charged Zen.
- **Preset consistency:** Balanced = 32 models. Non-preset `nvidia/*` models in DB (moonshotai, mistralai, qwen, meta) are from OpenCode's default cascade, not config drift.
- **Config churn:** **97 git commits in 90 days** — high churn; skip per-period matching.

## 1. Summary

**Window:** 2026-05-11 → 2026-08-09 (90d). **96,795 steps**, **6.9B tokens**, **108 models**, **35 agents**. DB cost est **$296.34** (Go pricing-model inflation + nvidia free-tier noise).

**Real costs (ground truth):**
- **Workspace billing:** **$127.82 spent** ($140 reloaded, $12.18 balance, 9 payments)
- Cloud aggregate (card-charged): $11.14
- Zen paid DB estimate: $94.31
- **Go cap: 0% monthly / 36% weekly** (recently reset)

**Active preset:** `balanced` (32 models, 11 roles, custom council config).
- **orchestrator:** longcat-2.0-free → minimax-m3 (nvidia/Go/opencode)
- **oracle:** big-pickle → deepseek-v4-pro (nvidia/Go/deepseek)
- **designer:** mimo-v2.5-free → minimax-m3 (nvidia/Go/opencode)
- **librarian/explorer/fixer:** free cascade → deepseek-v4-flash (paid)
- **council α/β/γ:** big-pickle / nemotron-free / mimo-v2.5-free primaries
- **skeptic:** nemotron-3-ultra-free → qwen3.7-plus (variant: max)

Config has 3 presets (balanced active, nvidia-free, opencode-zen-free). `image_routing: auto` — observer may trigger at runtime.

## 2. Findings

### Real Costs (Ground Truth)
| Source | Amount |
|--------|-------:|
| **Workspace billing (ground truth)** | **$127.82** |
| Cloud aggregate (card-charged Zen) | $11.14 |
| Zen paid DB estimate | $94.31 |
| Go-tier cap consumption | 0% monthly / 36% weekly |

### Go Cap Analysis
- **0% monthly / 36% weekly** (recently reset, no immediate pressure)
- **Top Go consumers by agent (DB est):** orchestrator $53.12 (47.6%), oracle $18.45 (17.8%), build $15.36 (14.8%)
- **Top Go models:** qwen3.7-plus (2,068), glm-5.1 (1,489, NOT in preset), deepseek-v4-flash-Go (1,453), mimo-v2.5 (961), minimax-m3 (959)
- `glm-5.1` is **#1 Go drain** at $37.94 — NOT in active preset, from legacy/non-balanced sessions

### Error Flags (n ≥ 100)
| Agent | Model | Provider | Rate | n | In Preset? |
|-------|-------|----------|------|---|-----------|
| orchestrator | deepseek-v4-pro | deepseek | 12.8% | 1,499 | Yes (oracle fallback) |
| orchestrator | nvidia/nemotron-3-ultra-550b-a55b | nvidia | 9.4% | 593 | No (default cascade) |
| explore | opencode/gpt-5.4 | opencode | 16.4% | 122 | No (default cascade) |
| plan | siliconflow/MiniMaxAI/MiniMax-M3 | siliconflow | 6.4% | 421 | No (default cascade) |

**orchestrator/deepseek-v4-pro 12.8% (1,499 steps):** Model is in preset as oracle/council.α fallback, but the error rate is in orchestrator agent which does NOT include this model. OpenCode's default cascade is routing it after orchestrator's free models exhaust. Real concern if the free cascade is too thin.

### Latency Flags (avg > 100s, n ≥ 100)
- **kimi-k2.6 (Go):** 361s avg, 158 steps — not in preset
- **nvidia/minimaxai/minimax-m3:** 267s avg, 2,011 steps — IN preset (orchestrator/designer fallback), free but slow
- **nvidia/nemotron-3-ultra-550b-a55b:** 245s avg, 593 steps — default cascade
- **opencode/minimax-m3-free:** 105s avg, 923 steps — free tier in preset

The free nvidia/minimax-m3 fallback at 267s is notable. When orchestrator's free cascade exhausts and this kicks in, expect ~4.5min/step latency.

### What's Working
- **~70% of steps on free models** (deepseek-v4-flash-free 18,663, mimo-v2.5-free 14,836, hy3-free 9,458, big-pickle 5,392)
- **deepseek-reasoner:** 7,745 steps at $35.61 est — best throughput/cost among paid
- No free model misconfigurations (all 5 free zen models show $0)
- Go cap recently reset, no immediate pressure

## 3. Insights

1. **Orchestrator = 47.6% of Go consumption** ($53.12 of $103.54 est). Free cascade is too thin (3 free models before Go fallback). If cap pressure rises, adding `hy3-free` and `deepseek-v4-flash-free` to orchestrator before `opencode-go/minimax-m3` is the single biggest lever.

2. **Daily burn has collapsed.** Last 7 days: $0.48 total. 30-day cost: $93.76 across 45,952 steps. The 90-day $296.34 is front-loaded (June 16 = $23.11 spike). Workspace billing is being fed by historical charges, not current burn.

3. **The 12.8% deepseek-v4-pro error in orchestrator is the default cascade, not the preset.** The model works fine in oracle/council.α roles. This is a symptom of orchestrator's free cascade being too thin — same root cause as #1.

4. **108 models, only 32 in preset.** The 76 non-preset models are OpenCode's default cascade (nvidia moonshotai/mistralai/qwen/meta, siliconflow, openrouter, github-copilot, ollama). All free. No action needed.

5. **Skeptic's `max` variant could be expensive when triggered.** DB shows only $1.53 across 333 steps so far, but `max` variant inflates per-step cost. Replacing `nemotron-3-ultra-free` primary with `opencode/qwen3.7-plus` (free zen) would eliminate Go fallback needs.

6. **Config churn masks historical error rates.** 97 commits in 90 days. Error/latency rates reflect multiple prior preset versions, not just the current one. Don't over-interpret.

## 4. Essential Questions

**Q1: What is the real cost?**
Workspace billing: $127.82. DB Zen paid estimate: $94.31 (undercounts due to older pre-window charges). Go cap: 0% monthly / 36% weekly (recently reset). Real split: ~$60+ for card-charged Zen over 9 reloads, plus Go subscription.

**Q2: Is the Go cap sustainable?**
Currently healthy (0% monthly, 36% weekly after reset). Risk: orchestrator alone = 47.6% of Go. If a heavy session lands on the orchestrator cascade exhausting to Go, the cap can move fast.

**Q3: Are preset primaries reliable?**
Yes. mimo-v2.5-free, big-pickle, deepseek-v4-flash-free, north-mini-code-free all show low error rates. The 12.8% deepseek-v4-pro error is in orchestrator (default cascade), not oracle/council.α where it's wired.

**Q4: What's the biggest optimization?**
Orchestrator's free cascade. Add `hy3-free` and `deepseek-v4-flash-free` before the Go fallback. At 4,926 Go steps / $53.12 est, a 40% reduction saves ~$21/mo of cap consumption.

**Q5: Is the data trustworthy?**
Mostly. 7-day daily log gap (acceptable), 5h hourly truncation today (notable), DB undercounts Zen paid vs workspace (unusual, explained by older pre-window charges). The 108-model footprint is dominated by OpenCode's default cascade, not config drift.

## 5. Recommendations

### Real Cost
- **Workspace billing (ground truth):** $127.82 spent — this IS your card total
- **Cloud aggregate:** $11.14 card-charged Zen — per-model breakdown only
- **Zen paid DB estimate:** $94.31 — relative signal, not bill
- **Go cap:** 0% monthly / 36% weekly — currently healthy

### Actions

1. **Add `hy3-free` and `deepseek-v4-flash-free` to orchestrator cascade** before `opencode-go/minimax-m3`. Current preset has only 2 free models (longcat-2.0-free, laguna-s-2.1-free) before the Go fallback. Adding 2 more could cut Go consumption 30-50% on orchestrator (~$20/mo cap saved).

2. **Investigate glm-5.1 (Go) $37.94 / 1,489 steps** — top Go drain, NOT in active preset. From old sessions. Tracking down the source could recover significant cap headroom.

3. **Skeptic: swap nemotron-3-ultra-free primary → opencode/qwen3.7-plus (free zen), remove opencode-go/qwen3.7-plus fallback.** The `max` variant makes per-step cost high; using free-zen primary eliminates Go needs for this role.

4. **No action on nvidia default cascade errors** (nemotron-3-ultra-550b-a55b 9.4%, minimax-m2.7 5.2%). These are from OpenCode's default provider cascade, not the user's preset. All free. Adding the user's preset to "fix" them would expand cascade unnecessarily.

5. **Monitor Go cap weekly.** Current 36% weekly just reset. If weekly trends up, demote non-essential Go fallbacks to opencode/ (free) for lower-priority roles.

### Go-Tier Model Breakdown
| Model | Steps | DB est. $ | In Preset? | Notes |
|-------|------:|----------:|-----------|-------|
| qwen3.7-plus | 2,068 | $9.14 | Yes | council.β, skeptic fallback |
| glm-5.1 | 1,489 | $37.94 | **No** | Top drain — investigate |
| deepseek-v4-flash (Go) | 1,453 | $1.28 | Yes | explorer/fixer fallback |
| minimax-m3 (Go) | 959 | $8.35 | Yes | orchestrator/designer fallback |
| kimi-k2.7-code | 802 | $16.97 | Yes | legacy role |
| glm-5.2 (Go) | 643 | $13.81 | No | old Zen |

## 6. Raw Data

### Top 5 Models by Estimated Cost
1. **opencode-go/glm-5.1:** est. $37.94, 1,489 steps (NOT in preset — top Go drain, legacy)
2. **deepseek/deepseek-reasoner:** est. $35.61, 7,745 steps (NOT in preset — best throughput/cost)
3. **opencode/glm-5.2:** est. $20.29, 518 steps (NOT in preset — old Zen paid)
4. **opencode/gpt-5.5:** est. $19.58, 158 steps (NOT in preset — old Zen paid)
5. **opencode-go/kimi-k2.7-code:** est. $16.97, 802 steps (IN preset — legacy role)

### NotebookLM Status
- **34/34 sources ready** ✓
- DeepSWE, SWE-rebench, OpenCode Go/Zen pricing refreshed
- **arena.ai refresh FAILED (HTTP 429)** — will retry next run
- LiveBench: 4.9d old (FRESH)

### Verdict
- Real cost: $127.82 card-spent, Go cap healthy
- Main lever: Expand orchestrator free cascade (~$20/mo cap savings potential)
- No critical errors in preset primaries
- Legacy cleanup: `glm-5.1` is the #1 cost model not in preset
